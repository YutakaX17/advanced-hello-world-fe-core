# Contributing

Create a short-lived `feature/`, `fix/`, `docs/`, or `chore/` branch and open a
pull request into `main`. Use Conventional Commits. Include type, component,
accessibility, package-contract, and browser-impact information.
